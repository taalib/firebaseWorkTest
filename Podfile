# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'firebaseWorkTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for firebaseWorkTest
  pod 'Firebase/Core'
  pod 'Firebase/Database'


  target 'firebaseWorkTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'firebaseWorkTestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
